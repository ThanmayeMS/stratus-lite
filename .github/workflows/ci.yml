name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    name: Repository Initialized
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Confirm project structure
        run: |
          test -f README.md
          test -f docs/architecture.md
          test -f docs/day-plan.md
